<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow cleartext (HTTP) traffic for development -->
    <!-- This is required for connecting to local development servers -->
    <!-- WARNING: This should be disabled or restricted in production builds -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <!-- Trust system certificates -->
            <certificates src="system" />
            <!-- Trust user-added certificates (for self-signed certs in development) -->
            <certificates src="user" />
        </trust-anchors>
    </base-config>
    
    <!-- Optional: Allow cleartext only for specific domains (more secure) -->
    <!-- Uncomment and customize if you want to restrict HTTP to specific IPs -->
    <!--
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">192.168.0.101</domain>
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
    </domain-config>
    -->
</network-security-config>

