## Decision Log

Record decisions that affect scope, UX, brand, or technology.

| Date | Decision | Context / Options Considered | Rationale | Impact | Owner |
|------|----------|------------------------------|-----------|--------|-------|
| 2025-01-27 | Android Framework: Kotlin Native | Kotlin Native (Jetpack Compose) / Flutter (Dart) / Java | Kotlin is official Android language, modern, performant. Jetpack Compose provides modern UI. Clean Architecture (MVVM) enables iOS-ready structure by sharing API layer and patterns. Better RTL/Hebrew support than Flutter. Aligns with Android-first approach; iOS app can be built later using same REST API with SwiftUI. | Project will use Kotlin with Jetpack Compose. Architecture will follow MVVM pattern with clear separation of concerns. Business logic in ViewModels, data layer uses Repository pattern. | TBD |
| 2025-01-27 | Backend Framework: PHP Laravel | PHP Laravel / Python FastAPI / Node.js Express / Go / Java Spring Boot | Laravel has excellent native MySQL support (meeting preference). Very cost-effective hosting options (important for 100k ILS budget). Rich ecosystem with built-in features (auth, validation, ORM). JWT libraries readily available. Mature, stable, good documentation. Python FastAPI considered but hosting typically costs more. | Backend will be Laravel application. Will use Eloquent ORM for database, Sanctum or JWT for authentication. API structure follows RESTful patterns defined in API spec. | TBD |
| 2025-01-27 | Backend Framework: Node.js + Express | Node.js + Express / Python FastAPI / Kotlin Ktor / Go / Java Spring Boot / PHP Laravel | Node.js chosen due to developer familiarity (no PHP experience). Excellent for REST APIs, large ecosystem, fast development cycle. TypeScript for type safety. Excellent MySQL support via Prisma or Sequelize. Same cost-effective hosting as PHP (~$10/month on VM). Perfect for API-first mobile backend. | Backend will be Node.js with Express.js framework and TypeScript. Will use Prisma ORM for MySQL database. Will use jsonwebtoken and Passport.js or similar for OAuth/JWT authentication. API structure follows RESTful patterns defined in API spec. | TBD |
| 2025-01-27 | Infrastructure: Azure VM | VPS (DigitalOcean/Linode/etc.) / Shared Hosting / AWS/GCP / Other Cloud / Azure | Selected Azure VM for backend deployment. Standard_B1s VM in uksouth region (~$10/month). Provides cost-effective solution with full control. Better than shared hosting for API performance. Cloud provider integration available if needed later. MySQL self-managed on VM initially. | Azure VM provisioned (dayparty-vm, IP: 172.167.43.172, region: uksouth). Ubuntu 22.04 LTS with Node.js 24.11.0, Nginx reverse proxy, PM2 process manager, MySQL 8.0.43, UFW firewall. Self-signed SSL configured for testing. | TBD |
| 2025-01-27 | Android Architecture Pattern: MVVM | MVVM / MVI / MVP / Clean Architecture | MVVM provides clear separation: View (Compose UI), ViewModel (business logic), Model (Repository/Data layer). Enables testability and maintainability. Aligns with Android best practices and Jetpack Compose. Repository pattern allows easy API abstraction and caching strategy. | Code structure: ui/ (Composables), viewmodel/ (ViewModels), repository/ (data access), model/ (data classes), network/ (API clients). Use StateFlow/Flow for reactive state management. | TBD |
| 2025-01-27 | Android Minimum SDK: API 24 (Android 7.0) | API 21+ / API 24+ / API 26+ / API 28+ | API 24 covers ~95%+ of Android devices while enabling modern features. Balances compatibility with feature access. Allows use of newer Android APIs without fragmentation concerns. | gradle.properties will set minSdkVersion 24, targetSdkVersion will be latest stable (34+). | TBD |
| 2025-01-27 | Android Networking: Retrofit + OkHttp | Retrofit+OkHttp / Ktor / Volley / Manual HttpURLConnection | Retrofit is industry standard for REST APIs, type-safe, integrates well with Kotlin coroutines. OkHttp provides HTTP client with interceptors for auth headers. Both have excellent documentation and community support. Enables easy JWT token refresh handling. | Will use Retrofit for API service interfaces, OkHttp for client configuration. Will add interceptors for JWT token injection and refresh logic. | TBD |
| 2025-01-27 | Android Dependency Injection: Hilt | Hilt / Koin / Dagger / Manual DI | Hilt is Google's official DI solution built on Dagger, integrates seamlessly with Android. Reduces boilerplate, provides Android-specific scopes (Activity, ViewModel). Better compile-time safety than Koin. Standard choice for modern Android apps. | Will use Hilt for dependency injection throughout app. Application, Activity, and ViewModel scopes will be configured. | TBD |
| 2025-01-27 | Backend Authentication: JWT + Passport.js | JWT (jsonwebtoken) + Passport.js / Auth0 / Firebase Auth | jsonwebtoken for JWT token generation/verification. Passport.js for OAuth strategies (Google, Facebook, Apple). Simple, flexible, widely used. Can migrate to managed auth service (Auth0) later if needed. | Backend will use jsonwebtoken for JWT tokens and Passport.js with OAuth strategies for social authentication. Tokens issued after OAuth callback, stored securely in Android app. | TBD |
| 2025-01-27 | Image/Video Handling: Android Media3 + Coil | Media3 / ExoPlayer / Coil / Glide | Media3 (formerly ExoPlayer) is modern, maintained for video playback. Coil is Kotlin-first, Compose-friendly image loading library, lighter than Glide. Both integrate well with Compose and coroutines. Coil handles caching, transformations efficiently. | Will use Coil for image loading/composables, Media3 for video playback in app. Video upload will use multipart/form-data via Retrofit. | TBD |
| 2025-01-27 | Database: MySQL 8+ (as specified) | MySQL 8+ / PostgreSQL / SQLite / Other | MySQL 8+ is specified in Creative Brief as preference. utf8mb4 character set required for Hebrew/RTL support. Prisma ORM provides excellent MySQL support with type safety. Same VM can host MySQL initially. | Database configured: MySQL 8.0.43 on VM. Database `dayparty` with utf8mb4_unicode_ci collation. 10 tables created from DDL (11-MySQL-DDL.sql). Prisma schema completed and validated. Backup script configured (~/backup-mysql.sh). | TBD |
| 2025-01-27 | Windows Command Line: No `&&` Operator | Windows CMD/PowerShell / Unix Shell | **CRITICAL FOR AI AGENTS**: Windows command line (CMD and PowerShell) does NOT support the `&&` operator for chaining commands like Unix/Linux shells. **MUST use `;` (semicolon) instead of `&&` in PowerShell**. Use `copy` instead of `cp`, and other Windows-specific commands. This workspace uses Windows PowerShell. | **IMPORTANT**: When running terminal commands in this Windows environment, NEVER use `&&`. ALWAYS use `;` (semicolon) for sequential commands in PowerShell. Example: `cd dir1 ; cd dir2` NOT `cd dir1 && cd dir2`. Use `copy` instead of `cp`. All command examples in documentation should use `;` for Windows compatibility. | TBD |
| 2025-11-10 | Mobile Framework: Flutter (Dart) | Flutter (Dart) / Kotlin Native (Jetpack Compose) / React Native | Flutter chosen for cross-platform development (Android + iOS from single codebase). Excellent RTL support for Hebrew, mature ecosystem, hot reload for fast development. Single codebase reduces maintenance overhead. Good performance and native feel. Provider pattern for state management. | Project uses Flutter with Dart. Architecture follows Provider pattern for state management. Cross-platform support enables iOS app with same codebase. Deep linking configured for OAuth callbacks (dayparty://auth/callback). | TBD |
| 2025-11-10 | External Video Link Support | Upload only / Upload + External Links | Implemented support for external video links (YouTube, Vimeo) in addition to video uploads. Allows users to paste video URLs instead of uploading files. Backend validates URLs, fetches metadata via oEmbed, stores normalized URLs and embed HTML. | Nodes support both `video_source='upload'` (Google Drive) and `video_source='external'` (YouTube/Vimeo links). POST /videos/preview endpoint validates and fetches metadata. Video link service handles URL normalization, provider detection, and oEmbed fetching. | TBD |
| 2025-01-27 | Rich Text Format: Delta JSON | HTML / Delta JSON / Markdown | Migrated from HTML format to Delta JSON (Quill Delta format) for rich text content. Delta is more efficient for Flutter rendering, provides better performance, and integrates natively with Flutter Quill editor. HTML was chosen initially but Delta provides better mobile performance and native Flutter integration. | Backend schema updated to include `'delta'` and `'html'` in `TextFormat` enum. Frontend uses `flutter_quill` package (^10.7.0) for editing and custom Delta renderer for display. Migration script (`html-to-delta.ts`) converts existing HTML content to Delta format. Custom renderer supports bold, italic, underline, strikethrough, links (clickable), and lists (numbered/bulleted). | TBD |


